#include <iostream>

int main()
{
	void insertionSort(int A[], int n) {
	int a;
	int A[5] = { 1,2,3,4,5,6 };

	for (int j=2 ; j<=n ; j++){
		key = A[j];
		i = j - 1;

	}

	while (i >= 0 && A[i] > key) {
		A[i + 1] = A[i];
		i = i - 1;
    }

	A[i + 1] = key;
       
    }

}